[package]
name = "castor-kernel"
version = "0.1.0"
edition = "2021"

[lib]
name = "castor_kernel"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.29.0", features = ["extension-module"] }
